'use client';
import { ProjectDetail } from '@/components/figma/ProjectDetail';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function Page({ params }: { params: { id: string } }) {
  return <ProjectDetail />;
}
