'use client';
import { BOQDetail } from '@/components/figma/BOQDetail';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function Page({ params }: { params: { id: string } }) {
  return <BOQDetail />;
}
